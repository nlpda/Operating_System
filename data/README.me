Data placeholders for measurements, logs, and audit evidence.

- `performance-template.csv`: start capturing metrics here or create new CSVs per test run.
- `audit/`: store Lynis logs, nmap outputs, sshd configs (before/after), firewall exports.
- `logs/`: raw command outputs captured during monitoring and testing.

Keep sensitive data out of the repo. Remove usernames/IPs you do not wish to publish.***
